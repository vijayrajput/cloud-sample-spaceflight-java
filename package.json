{
  "name": "spaceflight",
  "version": "0.1.0",
  "dependencies": {
    "@sap/cds": "2.x",
    "__spaceflight-model": "https://github.com/chgeo/test"
  },
  "scripts": {
    "__build": "cds build --clean && reuseTableData",
    "build": "cds build --clean"
  },
  "license": "ISC"
}
