{
  "name": "spaceflight",
  "version": "0.1.0",
  "dependencies": {
  },
  "devDependencies": {
    "npm-watch": "^0.3.0"
  },
  "watch": {
    "build": {
      "patterns": ["db", "srv"],
      "extensions": "cds",
      "inherit": true
    }
  },
  "scripts": {
    "build": "node node_modules/@sap/cds/bin/cds.js build --clean",
    "watch": "npm-watch"
  },
  "license": "ISC"
}
